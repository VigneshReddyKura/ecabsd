# ECABSD — pinned requirements
# Install order matters for PyG. Follow the README installation steps exactly:
#
#   pip install torch==2.2.2 torchvision==0.17.2 torchaudio==2.2.2 \
#       --index-url https://download.pytorch.org/whl/cpu
#
#   pip install torch-geometric==2.7.0
#
#   pip install torch-scatter torch-sparse torch-cluster \
#       --find-links https://data.pyg.org/whl/torch-2.2.2+cpu.html
#
#   pip install -r requirements.txt

# --- Web / API ---
# fastapi >=0.109.1 fixes PYSEC-2024-38 (request smuggling)
# python-multipart >=0.0.31 fixes multiple CVEs (2026-53538/39/40, 2026-40347, 2026-42561)
# starlette >=0.47.2 fixes PYSEC-2026-1941 (header injection / path traversal)
fastapi==0.115.6
uvicorn==0.27.0
python-multipart==0.0.31
starlette==0.41.3

# --- Core ML (versions must match the PyTorch wheel above) ---
# torch >=2.2.0 fixes PYSEC-2024-250/251/252 (code execution via crafted models)
torch==2.2.2
torch-geometric==2.7.0

# --- Biology ---
biopython==1.87
pydssp==0.9.1

# --- Numerics / utilities ---
numpy>=1.24.0,<2.0.0
scipy==1.12.0
PyYAML==6.0.3
# scikit-learn >=1.5.0 fixes PYSEC-2024-110
scikit-learn==1.5.2
pandas>=2.0.0
tqdm==4.66.4
psutil==5.9.8

# --- Visualisation ---
matplotlib==3.10.3
seaborn==0.13.2

# --- NLP (ESM-2 embeddings) ---
# transformers >=4.53.0 fixes all known 2024-2026 CVEs including deserialization issues
transformers==4.53.0

# --- CLI ---
typer==0.12.3
